name: uniir
channels:
  - defaults
  - conda-forge
  - pytorch
dependencies:
  - python=3.9
  - pip
  - pip:
      - python-dotenv
      - pycocoevalcap
      - omegaconf
      - numpy
      - torch
      - torchvision
      - wandb
      - ftfy
      - tqdm
      - regex
      - typeguard
      - datasets
      - transformers
      - git+https://github.com/openai/CLIP.git
      - timm
      - fairscale
      - opencv-python